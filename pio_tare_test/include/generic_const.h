#ifndef GENERIC_CONST_H
#define GENERIC_CONST_H

const char SENSOR_NAME[] = "demo_load_cell";
const unsigned long LOOP_SLEEP = 1000; // milliseconds to sleep each loop
const unsigned long STARTUP_DELAY = 5000; //millis to sleep after startup before looping

#endif